#include<stdio.h>
void main()

{
    // Factorial of accepted numbers
    
    int i,f=1,num;
    
    printf("Enter the numbers : ");
    scanf("%d",&num);
    
    for(i=1; i<=num; i++)
    
    {
        f = f * i;
    }
    
    printf("Factorial is : %d",f);
}
